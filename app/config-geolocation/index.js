

const initGeoLocation = (gsGeolocation, gsCurrentLocation, gsLocationManager, gsPlaceSearchManager, gsPlaceExplorerDataService, gsPlaceFilter) => {};


export default (ngModule) => ngModule.run(initGeoLocation);
